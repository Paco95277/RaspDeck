{
  "rows": 3,
  "cols": 5,
  "buttons": [
    { "id": "b1", "title": "Return", "icon": "◀", "action": {"type": "nav", "route": "/cheatsheet/python" } },
	  { "id": "b2", "title": "If Elif", "icon": "📝", "action": { "type": "insert", "text": "if exp1:\n\tcode1\nelif exp2:\n\tcode2\nelse:\n\tcode3\n" } },
    { "id": "b3", "title": "Shorthand If", "icon": "📝", "action": { "type": "insert", "text": "statementA if var1 > var2 else statementB\nvar = value1 if value1 > value2 else value2\n" } }
  ]
}